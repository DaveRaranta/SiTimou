import 'package:flutter/material.dart';

class ErrorKoneksiPage extends StatelessWidget {
  const ErrorKoneksiPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Center(
        child: Text("ERROR"),
      ),
    );
  }
}
